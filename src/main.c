/*
** EPITECH PROJECT, 2017
** my_ls
** File description:
** epitech
*/

int compile(int ac, char **av)
{
	DIR *dir;
	struct dirent *entry;

	while ((entry = readdir(dir) != NULL)) {}
	return (0);
}

int main(int ac, char **av)
{
	compile(ac, av);

	return (0);
}
